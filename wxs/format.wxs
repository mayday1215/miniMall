function countFromat(count){
  return count >= 10000 ? (count / 10000).toFixed(2) + "万" : count
}
function tallAndLow(boo){
  return boo ? "高" : "低"
}

module.exports={
  countFromat:countFromat,
  tallAndLow:tallAndLow
}